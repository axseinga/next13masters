fragment Cart on Order {
    id
    orderItems {
        id
        quantity
        total
        product {
            id
            name
            description
            price
        }
    }
}